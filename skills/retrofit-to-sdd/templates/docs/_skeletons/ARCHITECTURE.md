# 架构设计：<项目名>

> 系统当前真貌（HOW）。始终原地更新到现状；结构 / 机制变了就改它。

## 1. 定位与边界
<这个系统是什么、不是什么；与外部系统的边界在哪。>

## 2. 模块与依赖
<有哪些模块 / 组件，各自职责，依赖方向（避免循环依赖）。>

## 3. 数据模型
<核心实体、关键字段、关系；持久化形态。>

## 4. 接口
<对内 / 对外的关键接口；详细契约见 API.md，此处只给概览与定位。>

## 5. 关键机制
<支撑系统运转的核心机制：如并发、缓存、事务、推送、选举等。>

## 6. 部署
<运行形态、进程 / 服务拓扑、依赖的中间件与运行环境。>

## 7. 关键裁决与不做项
<影响架构的重大取舍（链接到对应 ADR）；明确"当前不做什么"。>
