# 接口契约：<项目名>

> 对外接口的单一真源。始终原地更新到当前契约。

## 1. 通用约定
<协议形态（REST / RPC / 库 API 等）、认证、版本、编码、分页等通用规则。>

## 2. 错误约定
<统一的错误返回结构、错误码 / 异常类型、语义说明。>

## 3. 端点 / 方法

### <名称>
- **方法 / 路径**：<如 GET /api/xxx，或方法签名>
- **请求**：<参数 / 请求体字段与含义>
- **响应**：<返回结构与字段含义>
- **错误**：<可能的错误码 / 异常与触发条件>

<按此结构逐个补充其余端点 / 方法。>
