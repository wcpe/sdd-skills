# 架构不变量（防架构漂移）

> 以下是本项目锁定的架构约束（依据 `docs/ARCHITECTURE.md` 与 `docs/adr/`）。**违反任一条即为架构漂移。**
> 确需改变某条 → 先写新 ADR 取代旧决策、经确认后再改；**禁止在代码里静默违背**。

> 本文件是模板：以下小节为常见的不变量类别，请把每节标题与正文替换为本项目 ARCHITECTURE / ADR 里真正锁定的约束；不适用的小节删除，缺的小节补上。

## 1. `<核心边界（如分层 / 领域划分 / 控制面与数据面 / 模块依赖方向）>`
> 填本项目的核心边界约束：哪些层 / 域之间只能单向依赖、谁不许碰谁、什么逻辑禁止越界写入。

## 2. 简单优先（`<列出本项目禁用的重型件>`）
> 填本项目明确禁止引入的重型组件（如某类中间件 / 框架 / 分布式组件等），以及当前阶段约定的轻量替代方案。需要时先走 ADR 再引入。

## 3. `<真源 / 一致性约束>`
> 填各类事实的唯一真源在哪、谁权威谁从属、哪些数据不得互为权威或互相阻塞。

## 4. `<技术栈锁定>`
> 填本项目锁定的语言 / 框架 / 关键库 / 运行形态。换栈 / 换框架 = 架构决策 → 走新 ADR，不擅自更换。

## 红线（出现即停止并先确认）
> 把上述各节里"一旦出现就必须停下来确认"的硬性禁止项浓缩成一行清单，例如：引入被禁的重型件 · 越界写入核心边界 · 破坏真源 / 一致性约束 · 擅自换栈 · 静默违背任一已接受 ADR。
